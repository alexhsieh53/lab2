#include <iostream>

using namespace std;

class BMI
{
	public:
		float cal(float c);
		void setw(float a);
		float getw();
		void seth(float b);
		float geth();
		
		private:
			float weight;
			float height;
 } ;
